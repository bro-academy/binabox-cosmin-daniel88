<nav class="nav">
	<ul class="nav__list">
		{% for item in index.items %}
			<li class="nav__item {% if item.page == page %}nav__link--active{% endif %}">
				<a href="{{ item.url }}" class="nav__link">{{ item.text }}</a>
			</li>
		</ul>
	</nav>
{% endfor %}
